<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.1">
    <title>SignUp | Login </title>
    <link rel="apple-touch-icon" sizes="57x57" href="/apple-icon-57x57.png">
    <link rel="apple-touch-icon" sizes="60x60" href="/apple-icon-60x60.png">
    <link rel="apple-touch-icon" sizes="72x72" href="/apple-icon-72x72.png">
    <link rel="apple-touch-icon" sizes="76x76" href="/apple-icon-76x76.png">
    <link rel="apple-touch-icon" sizes="114x114" href="/apple-icon-114x114.png">
    <link rel="apple-touch-icon" sizes="120x120" href="/apple-icon-120x120.png">
    <link rel="apple-touch-icon" sizes="144x144" href="/apple-icon-144x144.png">
    <link rel="apple-touch-icon" sizes="152x152" href="/apple-icon-152x152.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-icon-180x180.png">
    <link rel="icon" type="image/png" sizes="192x192"  href="/android-icon-192x192.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96x96.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/manifest.json">
    <meta name="msapplication-TileColor" content="#ffffff">
    <meta name="msapplication-TileImage" content="/ms-icon-144x144.png">
    <meta name="theme-color" content="#ffffff">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<link 
href="https://fonts.googleapis.com/css?family=Rubik" rel="stylesheet"/>
    <link href="css/materialize.min.css" rel="stylesheet"/>
    <link href="tabs.css" rel="stylesheet"/>
    <link href="inputs-color.css" rel="stylesheet"/>
    <link href="newChannelForm.css" rel="stylesheet"/>
  </head>
  <body>
    <header>
    <nav>
      <div>
        <h2>SUMO</h2>
        <a href="/" class="home"><i class="material-icons">home</i></a>
      </div>
      </nav>
    </header>
    <section>
      <div class="forgotPass"><a href="/resetpassword">Forgot password</a></div>
      
      <div class="<%=errors.mode === 'login' && errors.errors.length > 0 ? 'display warning-board':'none'%>">
          <p class="<%=errors.mode === 'login' && errors.errors.length > 0 ? 'display':''%>">Invalid phone number and password</p>
      </div>  
          <div class="<%=success.length > 0 ? 'warning-board success display':'none' %>">
        <% if(success.length > 0) { %>
          <p>you can now login!!!</p>
          <%}%>
      </div>
      <div class="form-body z-depth-2">
    <div class="row">
    <div class="col s12 m12 l12">
      <ul class="tabs">
        <li class="tab col s3 m6 "><a href="#signup" class="active">Signup</a></li>
        <li class="tab col s3 m6 ">
            <a href="#login">login</a></li>
      </ul>
    </div>
    <div id="signup" class="col s12">
      <form action='/createUserChannel' method="post">
            <input type="hidden" value="<%=csrfToken%>" name="_csrf">
            <div class="input wrapper input-field">
              <% const name = errors.errors.find(err=>err.param === 'name') || 'confirmed' %>
            <i class="material-icons prefix">account_circle</i>
              <label for="name">Name</label> 
              <input id="name" name="name" type="text"  style="border-bottom-color:<%= name!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>" >
              <% if(name.param === 'name' && errors.mode === 'signUp') { %>
                <div class="warn" style="display:<%= name!== 'confirmed' && errors.mode === 'signUp' ? 'block' : 'none' %>"><small style="color:<%= name!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>"><%=name.message%></small></div>
                <%}%>
            </div> 

            <div class="input wrapper input-field">
              <% const email = errors.errors.find(err=>err.param == 'email') || 'confirmed' %>
               <i class="material-icons prefix">email</i>
               <label for="email">Email</label>
              <input id="email" name="email" type="email" style="border-bottom-color:<%= email!== 'confirmed' && errors.mode === 'signUp'  ? 'red' : '' %>"> 
              <div class="warn" style="display:<%= email!== 'confirmed' && errors.mode === 'signUp' ? 'block' : 'none' %>"><small style="color:<%= email!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>"><%=email.message%></small></div>
            </div>

              <div class="input wrapper input-field">
                <% const phone = errors.errors.find(err=>err.param == 'phone') || 'confirmed' %>
               <i class="material-icons prefix">phone</i>
               <label for="phone">phone</label>
              <input id="phone" name="phone" type="number" required style="border-bottom-color:<%= phone!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>">
              <div class="warn" style="display:<%= phone!== 'confirmed' && errors.mode === 'signUp' ? 'block' : 'none' %>"><small style="color:<%= phone!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>"><%=phone.message%></small></div>
             </div>
             <% const pwd = errors.errors.find(err=>err.param == 'pwd') || 'confirmed' %>
             <div class="input wrapper input-field">
               <i class="material-icons prefix">lock</i>
              <label for="pwd">password</label> 
               <input id="pwd" name="pwd" type="password" required style="border-bottom-color:<%= pwd !== 'confirmed' && errors.mode === 'signUp'  ? 'red' : '' %>"> 
               <div class="warn" style="display:<%= pwd!== 'confirmed' && errors.mode === 'signUp' ? 'block' : 'none' %>"><small style="color:<%= pwd!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>"><%=pwd.message%></small></div>
              </div>

              <div class="input wrapper input-field">
              <i class="material-icons prefix">enhanced_encryption</i>
               <label for="cpwd">comfirm password</label> 
                <input id="cpwd" name="cpwd" type="password" required style="border-bottom-color:<%= pwd!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>">
                <div class="warn" style="display:<%= pwd!== 'confirmed' && errors.mode === 'signUp' ? 'block' : 'none' %>"><small style="color:<%= pwd!== 'confirmed' && errors.mode === 'signUp' ? 'red' : '' %>"><%=pwd.message%></small></div>
              </div>

              <div class="button wrap"><button type="submit" class="z-depth-2">Submit</button></div>
          </form>
      </div>
    <div id="login" class="col s12">
      <form action='/loginuser' method="post" autocomplete="off">
        <input type="hidden" value="<%=csrfToken%>" name="_csrf">
            <div class="input wrapper input-field">
               <i class="material-icons prefix">phone</i>
               <label for="phone">Phone</label> 
              <input id="phone" name="phone" type="number" > </div>
             <div class="input wrapper input-field">
              <i class="material-icons prefix">lock</i>
              <label for="pwd">password</label> 
               <input id="pwd" name="pwd" type="password" > </div>
              <div class="button wrap"><button type="submit">Submit</button></div>
          </form>
      </div>
  </div>
      </div>
    </section>
    <script src="js/materialize.min.js"></script>
    <script src="newChannelForm.js"></script>
  </body></html>